#include "replace.hpp"

std::string replace(std::string line, std::string to_replace, std::string replace_with)
{
    std::string new_string = "";
    int s_pos = line.find(to_replace);
    while (s_pos != std::string::npos)
    {
        
    }

}